# Copy to .env for local dev, or set as a Vercel environment variable.

# Formspree form id (from https://formspree.io/f/<id>) that powers the
# /contact page. Leave unset to show a "not configured yet" message instead
# of submitting.
VITE_FORMSPREE_ID=
